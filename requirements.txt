# Core web framework
Flask==3.1.1
Werkzeug==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.46
python-dotenv==1.0.1

# Document parsing
PyPDF2==3.0.1
pdfplumber==0.11.9
pdfminer.six==20251230
python-docx==1.2.0
docx2txt==0.9

# Resume scoring (rule-based + TF-IDF/BoW/LSA ensemble)
numpy==2.4.2
scikit-learn==1.7.1

# Optional: semantic models (SBERT + spaCy). Disabled by default
# (ENABLE_SEMANTIC_MODELS=false). Uncomment only where these load
# reliably; they add only 0.20 weight to the ensemble score.
# sentence-transformers==5.6.0
# spacy==3.8.11
